# ============================================================
# ADAS — Phase 3 model pipeline (dev PC / WSL2, NOT the phone)
# Covers: IDD -> YOLO conversion, YOLOv8n fine-tuning, TFLite export + verify.
# Install: pip install -r requirements.txt
# ============================================================

ultralytics>=8.1.0        # YOLOv8 train + TFLite export
tensorflow>=2.15.0        # Required by Ultralytics' TFLite export + check_tflite_scale.py
numpy>=1.24.0
Pillow>=10.0.0            # Image-size fallback in training/idd_to_yolo.py

# --- PyTorch (ROCm) — DO NOT install from default PyPI (that pulls CUDA/CPU) ---
# Reuse the SAME wheel source proven on this box (Commentator project, ROCm 7.2.4).
# Install it FIRST, in a dedicated ADAS venv, using --find-links (NOT --index-url):
#   pip install --find-links https://repo.radeon.com/rocm/manylinux/rocm-rel-7.2.4/ \
#       torch==2.10.0 torchvision==0.25.0
# Then `source training/env.sh` and verify the GPU:
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# See training/README.md §0 for the full (already-installed) system ROCm context.
